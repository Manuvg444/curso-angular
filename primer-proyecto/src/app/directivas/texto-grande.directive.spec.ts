import { TextoGrandeDirective } from './texto-grande.directive';

describe('TextoGrandeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextoGrandeDirective();
    expect(directive).toBeTruthy();
  });
});
