import { RolPermitidoDirective } from './rol-permitido.directive';

describe('RolPermitidoDirective', () => {
  it('should create an instance', () => {
    const directive = new RolPermitidoDirective();
    expect(directive).toBeTruthy();
  });
});
