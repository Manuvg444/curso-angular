import { ContadorClicksDirective } from './contador-clicks.directive';

describe('ContadorClicksDirective', () => {
  it('should create an instance', () => {
    const directive = new ContadorClicksDirective();
    expect(directive).toBeTruthy();
  });
});
